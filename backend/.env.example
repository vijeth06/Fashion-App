# VF-TryOn Backend Environment Configuration
# Copy this file to .env and update with your actual values

# ===========================================
# DATABASE CONFIGURATION (MongoDB Atlas)
# ===========================================
# Replace <username>, <password>, <cluster>, and <database> with your actual values
# Example: mongodb+srv://myuser:mypassword@cluster0.xyz123.mongodb.net/vf-tryon?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://vijeth:2006@wtlab.mongodb.net/vf-tryon?retryWrites=true&w=majority

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# Generate a strong secret key for JWT tokens
# You can use: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\"
JWT_SECRET=your-super-secret-jwt-key-replace-this-with-a-strong-random-string

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
# Maximum file size in bytes (10MB = 10485760)
MAX_FILE_SIZE=10485760

# Upload directory
UPLOAD_DIR=./uploads

# ===========================================
# EXTERNAL SERVICES (Optional)
# ===========================================
# Firebase Admin SDK (if using Firebase Auth)
# FIREBASE_SERVICE_ACCOUNT_KEY=path/to/serviceAccountKey.json

# AWS S3 (if using for file storage)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# ===========================================
# API CONFIGURATION
# ===========================================
API_VERSION=v1
API_PREFIX=/api

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=info